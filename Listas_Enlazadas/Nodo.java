class Nodo {
    int dato;       // Valor almacenado en el nodo
    Nodo siguiente; // Referencia al siguiente nodo

    // Constructor
    public Nodo(int dato) {
        this.dato = dato;
        this.siguiente = null;
    }
}